import { defineStore  } from "pinia"

export const useCaseStore = defineStore("caseId", {
    // case ID is current case to display and working
    state: ()=> {
        return {
        //access backend to get an Case object

        }
    }
})
